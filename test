There is something that is lacking
- The contrast is lacking
- Highlight what they do (hero section)
- Have elements that don't require much human interaction
- UIs that look appealing
- What does someone need to know to click the button or something...
- New design

- *** Emotional Aspect ***

- Stick to the sitemap
- What is the goal of each page
	- If it is not coming out clearly then we should 
	
- Gallery view of the project
- Telling the story is the primary focus of the website
- 


let mybutton = document.getElementById("myBtn");

// When the user scrolls down 20px from the top of the document, show the button
window.onscroll = function() {scrollFunction()};

function scrollFunction() {
  if (document.body.scrollTop > 20 || document.documentElement.scrollTop > 20) {
    mybutton.style.display = "block";
  } else {
    mybutton.style.display = "none";
  }
}

// When the user clicks on the button, scroll to the top of the document
function topFunction() {
  document.body.scrollTop = 0; // For Safari
  document.documentElement.scrollTop = 0; // For Chrome, Firefox, IE and Opera
}